import { Text, View } from "react-native";
import { styles } from "./index.styles";

const TagNew = () => {
    return (
        <View style={styles.container}>
            <Text style={styles.name}>Mới</Text>
        </View>
    );
}

export default TagNew;