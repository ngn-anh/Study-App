import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: "#ff0000ff",
        borderRadius: 50,
    },
    name: {
        fontSize: 12,
        color: "#FFFFFF"
    }
});