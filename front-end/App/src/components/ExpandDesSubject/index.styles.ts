import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    text: {
        fontSize: 14,
        color: "#fff",
        lineHeight: 20,
    },
    toggle: {
        color: "#FFD700",
        marginTop: 4,
        fontWeight: "500",
    },
});